generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model SystemState {
  key       String   @id
  payload   Json
  updatedAt DateTime @updatedAt
}
